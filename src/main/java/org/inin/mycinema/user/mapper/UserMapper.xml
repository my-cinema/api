<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.inin.mycinema.user.mapper.UserMapper">
        <insert id="insertUser" parameterType="Map" useGeneratedKeys="true" keyProperty="user_no">
            INSERT INTO USER (
                               user_name
                             , user_email
                             , user_status
                             , access_token
                             , refresh_token
                             , update_dt
            ) VALUES (
                       #{user_name}
                     , #{user_email}
                     , '00'
                     , #{access_token}
                     , #{refresh_token}
                     , DATE_FORMAT(now())
                     )
        </insert>

</mapper>